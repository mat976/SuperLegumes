{% extends 'base.html.twig' %}

{% block title %}Nouveau Héros
{% endblock %}

{% block body %}
	<h1>Créer un nouveau héros</h1>

	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button class="btn btn-primary">{{ button_label|default('Sauvegarder') }}</button>
	{{ form_end(form) }}

	<a href="{{ path('app_heros_index') }}">Retour à la liste</a>
{% endblock %}
