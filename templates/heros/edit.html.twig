{% extends 'base.html.twig' %}

{% block title %}Modifier Héros
{% endblock %}

{% block body %}
	<h1>Modifier Héros</h1>

	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button class="btn btn-primary">{{ button_label|default('Mettre à jour') }}</button>
	{{ form_end(form) }}

	<a href="{{ path('app_heros_index') }}">Retour à la liste</a>

	{{ include('heros/_delete_form.html.twig') }}
{% endblock %}
